<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.molice.oneingdufs"
    android:versionCode="1"
    android:versionName="1.00" >

    <uses-sdk android:minSdkVersion="7" />
    <!-- 添加网络访问权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/gdufsTheme">
        
        <service
            android:name=".services.Notification"
            android:process=":notification"></service>
        
        <activity
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:name=".activities.MainActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- 注册 RegisterActivity -->
        <activity
            android:name=".activities.RegisterActivity"></activity>
        <!-- 登录 LoginActivity -->
        <activity
            android:name=".activities.LoginActivity"></activity>
        <!-- 用户中心 HomeActivity -->
        <activity
            android:name=".activities.HomeActivity"></activity>
        <!-- 校园生活 LifeActivity -->
        <activity
            android:name=".activities.LifeActivity"></activity>
        <!-- 用户中心 -->
            <!-- 用户中心-个人信息 UserInfoActivity -->
        <activity
            android:name=".activities.UserInfoActivity"></activity>
    </application>

</manifest>